@page
@model RecipeMate.Pages.Recipe
@using static RecipeMate.Models.Utilities.StringExtensions

@{
    ViewData["Title"] = "Recipe";
}

<div class="text-center">
    <div class="row row-cols-1 row-cols-md-4 g-4">
        @if (Model.Recipes == default)
        {
            <!-- Make sure you show a message if there are no recipes to display. -->
            <p>There are no recipes to display.</p>
        }
        else
        {
            @foreach (var recipe in Model.Recipes)
            {
                <div class="col">
                    <div class="card h-100 w-75">
                        <img src="@recipe.ImageUrl" class="card-img-top" alt="@recipe.Title">
                        <div class="card-body">
                            <h5 class="card-title">@recipe.Title</h5>
                            <a asp-page="/RecipeDetails" asp-route-id="@recipe.Id" asp-route-title="@recipe.Title.ToSlug()" class="btn btn-primary">View Recipe</a>
                        </div>
                    </div>
                </div>
            }
        }
    </div>
</div>
